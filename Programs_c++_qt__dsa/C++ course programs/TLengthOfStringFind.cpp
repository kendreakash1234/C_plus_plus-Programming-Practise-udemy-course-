#include <iostream>
#include <cstring>

using namespace std;

int main()
{
    char s[50];
    cout<<"Enter the string"<<endl;
    cin.getline(s,50);
    cout<<"Length of string "<<strlen(s);

    return 0;
}
